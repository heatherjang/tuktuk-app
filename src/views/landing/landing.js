// import { mapGetters, mapActions } from 'vuex'

export default {
  name: 'landing-page',
  components: {
  },

  props: [],

  data () {
    return {
    }
  },

  computed: Object.assign({},
    // -- State Actions -------
    // mapGetters({

    // }),
    {
    }
  ),
  methods: Object.assign({},
    // -- State Actions -------
    // mapActions({
    // }),
    {
    }
  ),

  created () {
  },

  mounted () {
  },
  watch: {
  }
}
